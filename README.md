# PlayerStorage
PlayerStorage is a library that allows you to automatically create different versions of any storage that are linked to indiviusal players automatically. This pack uses a system based on advancements in order to track what storage belongs to each player and as such should be unaffected by events like player name changes.
